# ManabuTalkApp (まなぶトーク) HANDOFF

## ビルド環境(固定・変更しないこと)
- AGP 8.5.2 / Kotlin 1.9.24 / Gradle 8.9 (GitHub Actionsでダウンロードして使用、wrapperなし)
- minSdk 26 / compileSdk・targetSdk 34 / Java 17
- 依存ライブラリゼロ、XMLレイアウトなし、全画面Kotlinプログラマティック構築
- debug.keystore を commit 済み(署名の安定化用、storePassword/keyPassword は "android")
- パッケージ名: com.appathy.manabutalk
- GitHubアカウント: Sekiguchi-Takashi / リポジトリ名(想定): ManabuTalkApp

## アプリ概要
AI対話形式の学習アプリ。起動のたびに時間帯別の挨拶(おはよう/こんにちは/こんばんは)
から始まり、アバターとの雑談 → 5教科選択 → 3学習モード → テーマ/範囲/コメント入力
→ 実行、という流れ。

## 画面構成 (MainActivity.kt 内、関数でビュー切り替え)
1. `showGreetingScreen()` — 時間帯別挨拶 + 雑談セリフ(ランダム) + 簡易チャット
   (今はキーワードAIではなく固定リプライのプール。将来ここに本物のAI応答を差し込む)
   常時「べんきょうする」ボタンで画面2へ。
2. `showSubjectScreen()` — 国語/算数/理科/社会/情報 の5ボタン。
   `subjectThemes` マップの value が空リストの教科は無効化(グレーアウト)。
   現状「情報」のみ有効(テーマ = ["セキュリティ"])。
3. `showModeScreen()` — 教えてもらう/問題を解く/テストする の3ボタン。
4. `showFormScreen()` — テーマ(Spinner) / 範囲(RadioGroup: 広い・中間・狭い) /
   コメント(EditText, 20文字制限, TextWatcherでカウンタ表示) を入力。
   3つとも入力済みで実行ボタンが有効化(`validate()`)。
   - 「教えてもらう」→ `openYoutubeSearch()` でテーマ+範囲+コメント+"解説" を
     クエリにしてYouTube検索結果ページをACTION_VIEWで開く(YouTubeアプリ優先)。
   - 「問題を解く」「テストする」→ 現状 Toast で開発中を表示するだけのプレースホルダー。

## 今後の拡張ポイント
- **AIアバター本体**: 現状アバターの見た目は未実装(テキスト吹き出しのみ)。
  検討中とのことなので、キャラクターデザイン確定後に顔画像やアニメーションを追加。
- **雑談のAI化**: `chatReplies` は固定文言のプール。実際のAI応答に差し替える場合、
  BonsaiApp で構築したオンデバイスLLM(llama.cpp + gguf)や外部API呼び出しを流用可能。
- **問題を解く/テストする**: AIによる問題生成ロジックが必要(未設計)。
  テーマ/範囲/コメントの3パラメータをプロンプトに渡してAIに問題文・選択肢・正解を
  生成させる設計が有力。
- **他教科の追加**: `subjectThemes` に国語/算数/理科/社会のテーマリストを追加するだけで
  ボタンが自動的に有効化される設計にしてある。
- **YouTube検索クエリの調整**: 現在は `テーマ 範囲 コメント 解説` を単純結合。
  ヒット率を見ながら文言を調整すると良い。

## 既知の注意点(他プロジェクト共通の落とし穴)
- `git init` は必ずプロジェクトフォルダ内で実行すること(ホームディレクトリで実行すると
  トークン露出のリスクや GH013 Push Protection の原因になる)。
- `git remote add origin` を忘れずに。
- unzip時は `-o` フラグを忘れないこと。

## 過去問機能 (v1.1で追加、情報/セキュリティ)
- データは `QuestionData.kt` に集約。出典はIPA公式過去問(許諾・使用料不要)。
- **問題を解く**: `QuestionData.am2R6Autumn` からランダムに1問出題(`showRandomQuestionScreen`)。
- **テストする** → 午前Ⅱ / 午後 を選択(`showTestKindScreen`)。
  - 午前Ⅱ: `am2ByYear` から年度選択 → その年度の問題を順番に出題(`showAm2TestQuestion`)。
  - 午後: `pmExams` から年度選択 → IPA公式の問題PDF/解答例PDFをブラウザで開く。
- 出題共通画面 `showQuestionScreen`:
  - `choices != null` の問題 → 4択タップで自動採点(正解=緑/誤り=赤)＋解説表示。
  - `choices == null` の問題 → 「答えを見る」で正解＋解説を表示する一問一答(自己採点)。

## 過去問データの現状と拡張方法(重要)
- 現在の収録(すべて4択・自動採点・選択肢完備、正解はIPA公式解答例と照合済み):
  - **令和7秋 / 令和7春 / 令和6秋 / 令和6春 / 令和5秋 / 令和5春 の午前Ⅱ 各全25問(計150問)**
  - 午後PDF: 令和7秋・令和7春・令和6秋・令和6春・令和5秋・令和5春(令和5春は午後Ⅰ/午後Ⅱ)。
  - **午前Ⅰ(共通): 令和7秋・令和7春・令和6秋・令和6春 各全30問(計120問)**(AP・高度共通の基礎科目。セキュリティとは別テーマ)。
- **選択肢を推測で埋めない**こと。学習ツールとして誤答選択肢の捏造は有害。
  4択化するときはIPA公式問題冊子PDF等から実際の選択肢を転記する。
  (公式問題冊子PDFは検索エンジンに載らずweb_fetch不可だった。手元にPDFがあれば転記可能)
- **午前Ⅱに問題を足す**: `QuestionData.Question` を追記し、`am2ByYear` に年度エントリを追加。
- **午後に年度を足す**: `pmExams` に `PmExam` を追加。PDF URLの命名規則は
  `.../{dirId}-att/{YYYYrNNx}_sc_pm_qs.pdf`(問題) と `..._pm_ans.pdf`(解答例)。
  x = a(秋)/h(春)。2023春以前は午後が午後1/午後2に分かれ `_pm1_` `_pm2_` になる。
  年度ごとの dirId(例: 令和6=m42obm000000afqx / 令和5=ps6vr70000010d6y)に注意。

## バージョン履歴
- v1.0: 初回プロトタイプ。挨拶+雑談画面、5教科選択(情報のみ有効)、3モード選択、
  テーマ/範囲/コメント入力フォーム、「教えてもらう」のYouTube検索連携を実装。
- v1.1: 情報/セキュリティに過去問機能を追加。問題を解く(ランダム出題)、テスト(午前Ⅱ年度別の
  自動採点/一問一答、午後は年度別に公式PDF)。令和6年秋 午前Ⅱ 全25問を収録(4択2問+一問一答23問)。
- v1.2: 令和7年秋 午前Ⅱ 全25問を4択・自動採点(選択肢完備)で追加。正解はIPA公式解答例と照合済み。
  問題を解くは全収録年度からランダム出題に変更。午後PDFに令和7秋を追加。
- v1.3: 令和6秋の残り23問を公式問題冊子PDFから4択化(全25問4択に)。令和6春 午前Ⅱ 全25問を
  4択・自動採点で新規追加。3年度すべて選択肢完備・自動採点に。正解はIPA公式解答例と全問照合済み。
- v1.4: 令和5年秋・令和5年春の午前Ⅱ 各25問を4択・自動採点で追加(計125問=5年度)。午後PDFに
  令和5春(午後Ⅰ/午後Ⅱ)を追加。正解はIPA公式解答例と全問照合済み。
- v1.5: 情報/テーマに「午前Ⅰ(共通)」を新設(A案)。令和7年春 午前Ⅰ 全30問を4択・自動採点で追加。
  問題を解く・テストにテーマ選択(セキュリティ午前Ⅱ / 午前Ⅰ共通)を追加。データは am1ByYear で管理し、
  年度追加は am1ByYear に追記するだけ。正解はIPA公式解答例と全問照合済み。
- v1.6: 午前Ⅰ(共通)に令和7年秋・令和6年春の各30問を追加(計3年度90問)。正解はIPA公式解答例と全問照合済み。
  年度追加は am1ByYear への追記のみ(UIは変更不要)。
- v1.7: 令和7春 午前Ⅱ 25問(6年度150問に)と令和6秋 午前Ⅰ(共通)30問(4年度120問に)を追加。
  午後PDFに令和7春を追加。正解はIPA公式解答例と全問照合済み。総収録270問。
